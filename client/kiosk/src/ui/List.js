// import Item from "./Item"

function List(props) {
    return <ul>
        {/* {props.meetups.map(meetup => (
            <Item 
            key={meetup.id}
            image={meetup.image}
            />
        ))} */}
    </ul>
}

export default List